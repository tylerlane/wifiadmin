<h1>New disconnect</h1>

<% form_for(@disconnect) do |f| %>
  <%= f.error_messages %>
    <%= render :partial => 'form', :locals => { :f => f } %>
  <li>
    <%= f.submit "Create" %>
  </li>
</ol>
<% end %>
</fieldset>

<%= link_to 'Back', disconnects_path %>
